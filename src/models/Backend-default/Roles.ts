export enum Roles {
    MASTER,
    ADMIN,
    COORDINATOR,
    CUSTOMER,
    ATTENDANT,
}

export enum RolesColors {
    MASTER = "danger",
    ADMIN = "warning",
    COORDINATOR = "primary",
    CUSTOMER = "secondary",
    ATTENDANT = "info",
}